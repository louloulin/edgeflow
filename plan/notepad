@ai_gateway_plan.md 按计划实现相关功能，实现完成，增加测试验证，验证通过后更新 @ai_gateway_plan.md 标记实现的功能，按计划实现ui功能

@plan2.md  按计划实现相关功能，实现完成，增加测试验证，验证通过后更新 @plan2.md  标记实现的功能，中文说明

@xiufu.md 按照这个计划配置cargo test 分析异常修复问题，修复后，更新 @xiufu.md 标记实现的功能

@xf2.md 按这个计划实现改造功能，并，删除，不需要的代码，完成后更新 @xf2.md 标记实现的功能


@plugin.md 按照计划，分析目前的插件的实现，实现相关插件改造，实现后增加测试，测试验证通过后更新 @plugin.md 标记实现的功能


@plugin.md 按照计划，分析目前的插件的实现，实现相关插件改造，实现后增加测试，测试验证通过后更新 @plugin.md 标记实现的功能，将所有的插件使用最新的插件体系实现，不要增加适配，而是直接修改现有的插件，改造成最新的插件模式


@implementation_issues.md 按照整个计划和方案，修复问题，修复后增加验证，验证通过 更新 @implementation_issues.md 标记实现的功能



请基于 `plan/plugin.md` 文档中的计划，对 Proksi 项目的插件系统进行全面现代化改造。具体要求如下：

## 主要任务
1. **分析现状**：检查当前所有插件的实现状态，识别哪些插件还未采用最新的 Plugin trait 架构
2. **直接改造**：将所有插件直接修改为最新的插件模式，不要创建适配器或兼容层
3. **统一架构**：确保所有插件都实现标准的 Plugin trait，包括：
   - `name()` 方法
   - `metadata()` 方法（包含优先级、描述等）
   - `handle_request()` 和 `handle_response()` 方法
   - `start()` 和 `stop()` 生命周期方法
4. **测试验证**：为每个改造的插件添加或更新单元测试，确保功能正确性
5. **文档更新**：改造完成后更新 `plan/plugin.md`，标记已实现的功能

## 技术要求
- 使用统一的错误处理（PluginError）
- 实现标准化的日志记录（使用 tracing）
- 支持插件优先级系统
- 保持向后兼容性，避免破坏性变更
- 所有测试必须通过（目标：100% 通过率）

## 输出要求
- 使用中文进行说明和注释
- 提供详细的改造过程说明
- 回答插件系统是否支持 WebAssembly (WASM) 的问题
- 创建改造总结报告

## 验收标准
- 所有插件都采用最新的 Plugin trait 架构
- 测试套件 100% 通过
- 文档完整更新
- 零破坏性变更